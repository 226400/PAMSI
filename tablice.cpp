#include "../inc/tab.hh"



int main (){
  using namespace std;
  


  void newtab::add_elem (int elem){
    if (size<SIZE) {
      tab[size++]=elem;
      else if(size>=SIZE)
	int*nowy = new int[size+1]

	  for(int i=0;<=(size-1); i++){
	    nowy[i]=tab[i];
	  }

      nowy[size+1]=elem;
      size++;

	delete[]tab;
      tab=nowy;
    }
  }
  void newtab::show_elem (void){

  }
  
  return 0;
}
# PAMSI
